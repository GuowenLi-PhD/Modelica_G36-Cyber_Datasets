within ;
package FaultInjection "Fault injection in Modelica models"

annotation (uses(
    Modelica_Synchronous(version="0.93.0"),
    Buildings(version="7.0.0"),
    Modelica(version="3.2.3")),
  version="1.0.0",
  conversion(noneFromVersion=""));
end FaultInjection;
